# PostgreSQL Database Configuration
# These values are used by Docker Compose
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update the values below if needed (defaults work for local development)
# 3. Run: docker-compose up --build

# PostgreSQL container environment variables
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=universedb

# ASP.NET Core connection string override
# This overrides appsettings.json ConnectionStrings:UniverseDatabase when running in Docker
# Format: Host=<service_name>;Port=5432;Database=<db_name>;Username=<user>;Password=<password>
ConnectionStrings__UniverseDatabase=Host=postgres;Port=5432;Database=universedb;Username=postgres;Password=postgres

# PRODUCTION NOTES:
# - Never commit .env files to version control (already in .gitignore)
# - Use strong passwords in production environments
# - Consider using secret managers (Azure Key Vault, AWS Secrets Manager, HashiCorp Vault)
# - Use managed identities where possible
# - Rotate credentials regularly
